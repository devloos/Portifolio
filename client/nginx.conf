server {
  listen 8080;

  location /api {
    proxy_pass https://symfony-server-production-63e8.up.railway.app;
  }

  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /index.html =404;
  }
  
  include /etc/nginx/extra-conf.d/*.conf;
}